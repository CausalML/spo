seed: 1
n_actions: 10
feat_dim: 10
n_pairs: 200000
n_val_calib: 10000
n_test: 50000
hidden_sizes: [128, 128, 64]
activation: "gelu"
learning_rate: 3e-4
weight_decay: 1e-4
batch_size: 2048
train_steps: 10000
eval_every: 1000
link: "double_sigmoid"   # non-standard link for data gen
beta_sigmoid_a: -1.25
beta_sigmoid_b: 1.25
kappa: 0.2             # KL budget
sigma_smsspo: 0.1      # initial σ; will be annealed
h_espo: None           # kernel bandwidth; can be annealed
save_dir: "outputs/sim"

